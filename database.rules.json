{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid",
        "role": {
          ".validate": "newData.val() === 'merchant' || newData.val() === 'customer'"
        }
      }
    },
    "phones": {
      "$phone": {
        ".read": "true",
        ".write": "auth != null && (!data.exists() || data.val() === auth.uid) && newData.val() === auth.uid"
      }
    },
    "otps": {
      "$phone": {
        ".read": "true",
        ".write": "!data.exists() && newData.hasChildren(['otp', 'expiresAt']) && newData.child('otp').val().matches(/^\\d{6}$/) && newData.child('expiresAt').val() > now"
      }
    },
    "products": {
      "$uid": {
        ".read": "true",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "offers": {
      "$uid": {
        ".read": "true",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "shop": {
      "$uid": {
        ".read": "true",
        ".write": "auth != null && auth.uid === $uid",
        "latitude": {
          ".validate": "!newData.exists() || newData.isNumber()"
        },
        "longitude": {
          ".validate": "!newData.exists() || newData.isNumber()"
        }
      }
    },
    "password_resets": {
      "$phone": {
        ".read": "true",
        ".write": "!data.exists() && newData.child('status').val() === 'pending'"
      }
    },
    "analytics": {
      "$vendorUid": {
        ".read": "auth != null && auth.uid === $vendorUid",
        ".write": "auth != null && auth.uid === $vendorUid"
      }
    },
    "chats": {
      "$firstId": {
        ".read": "auth != null && auth.uid === $firstId",
        ".write": "auth != null && auth.uid === $firstId",
        "$secondId": {
          "messages": {
            "$messageId": {
              ".write": "auth != null && (auth.uid === $firstId || auth.uid === $secondId)"
            }
          },
          "lastMessage": {
            ".write": "auth != null && (auth.uid === $firstId || auth.uid === $secondId)"
          }
        }
      }
    }
  }
}
