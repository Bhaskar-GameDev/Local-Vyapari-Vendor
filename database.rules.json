{
  "rules": {
    "users": {
      "$uid": {
        ".read": "auth != null && auth.uid === $uid",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "phones": {
      "$phone": {
        ".read": "true",
        ".write": "auth != null"
      }
    },
    "otps": {
      "$phone": {
        ".read": "true",
        ".write": "true"
      }
    },
    "products": {
      "$uid": {
        ".read": "true",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "offers": {
      "$uid": {
        ".read": "true",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "shop": {
      "$uid": {
        ".read": "true",
        ".write": "auth != null && auth.uid === $uid"
      }
    },
    "password_resets": {
      "$phone": {
        ".read": "true",
        ".write": "true"
      }
    }
  }
}
